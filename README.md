# CRUD-H2
A simple Spring Boot project demonstrating CRUD (Create, Read, Update, Delete) operations using H2 in-memory database. Includes RESTful APIs for managing data with JPA and Hibernate integration.
